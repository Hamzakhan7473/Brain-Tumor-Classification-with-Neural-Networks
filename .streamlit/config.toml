# HealthTech MRI Report — 2024-style (glassmorphism, dark mode in-app)
# Light defaults; dark mode toggle in sidebar overrides via custom CSS

[theme]
primaryColor = "#0d9488"
backgroundColor = "#fafbfc"
secondaryBackgroundColor = "#ffffff"
textColor = "#1e293b"
font = "sans serif"

[server]
headless = true

[browser]
gatherUsageStats = false
